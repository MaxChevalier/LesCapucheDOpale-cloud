{
  "name": "azure-functions-logs",
  "version": "1.0.0",
  "description": "Azure Functions for receiving and processing application logs",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@azure/data-tables": "^13.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "azure-functions-core-tools": "^4.x",
    "rimraf": "^5.0.5",
    "typescript": "^5.x"
  }
}
